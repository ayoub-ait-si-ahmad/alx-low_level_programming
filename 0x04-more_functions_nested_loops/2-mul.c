#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two integers
 * @a: the input
 * @b: the input
 * Return: multiplie of a and b
 */
int mul(int a, int b)
{
	int multi;

	multi = (a * b);
	return (multi);
}
